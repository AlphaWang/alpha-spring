spring.cloud.nacos.config.server-addr=127.0.0.1:8848
# Nacos dataId format:
#   ${prefix}-${spring.profile.active}.${file-extension}
#   prefix is `spring.application.name` by default
spring.application.name=cart_front_api